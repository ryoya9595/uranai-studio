/* ============================================================
   星詠ノ書 — 設定ファイル（ここだけ書き換えればOK）
   ============================================================ */
window.HOSHIYOMI_CONFIG = {
  /* Cloudflare Worker（Proxy）のURL。
     Workerをデプロイしたら、その公開URLをここに貼る。
     例: "https://hoshiyomi-proxy.xxxx.workers.dev"
     空のままなら、体験版はローカル鑑定文で動作する（APIは呼ばない）。 */
  PROXY_URL: "",

  /* 使用するGeminiモデル（安価で高速な flash 系を推奨） */
  MODEL: "gemini-2.0-flash",

  /* 体験期間の終了スイッチ（手動）。
     true にすると、自前APIキー未設定の人は expired.html（APIの設定案内）へ誘導され、
     かつ配布者のProxy（＝あなたのAPIキー）は一切使われなくなる。
     ※ことりさんが「終了して」と指示したら、Claude Code がここを true にして push する。 */
  EXPIRED: false,

  /* （任意）自動終了日。"2026-07-22" のように入れると、その日の0時以降は自動で体験終了になる。
     ウェビナーごとに「開催日＋14日」を入れておくと、手動切り替え不要で自動的に案内へ差し替わる。
     空 "" なら無効（＝EXPIRED の手動切り替えのみ）。 */
  EXPIRE_DATE: "",
};
